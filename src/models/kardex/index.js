/**
 * @typedef {import('./identifier').KardexIdentifier} Identifier
 * @typedef {import('./book').KardexBook} Book
 */

export class Kardex {
  /** @type {Identifier} */
  identifier;

  /** @type {Book} */
  book;
}
